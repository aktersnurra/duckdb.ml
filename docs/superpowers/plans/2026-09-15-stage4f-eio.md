# Stage4f Eio checklist

Base: accepted Eio `bfcf198d`; retain its pool, Bridge, cancellation and shutdown lifecycle unchanged.

- [x] **API:** publish direct-style typed query, owned fold, whole-request ingestion, local Parquet fold/export interfaces; implement only private Eio worker-owner operations using existing Bridge/core; add a real five-operation roundtrip test and compile it with the pinned Eio stack.
- [x] **QUERY_INGEST (independently reviewed and parent-accepted):** source-named Eio query/fold/ingestion tests cover selected native cancellation and successful holds, worker TLS/raw callback frame, ordered owned chunks/decoding, rollback/explicit and actual 204800-row automatic flush, metadata race, and one Operation worker. Five behavior mutants fail unchanged assertions and restore green. Focused Eio coverage/strict C passed; current sanitizer selectors are handed to INTEGRATION.
- [x] **PARQUET (independently reviewed and parent-accepted):** ordinary/generated source-named owned multi-file, callback/TLS and file-error tests; selected first-file retirement and execute/fetch/COPY/reservation/link/unlink/destruction holds with real caller cancellation and heartbeat; same-request published contents survive. Five real-behavior mutants fail unchanged assertions and restore green. Focused Eio/strict-C checks pass, with one retained intermittent old FIFO timeout and passing focused retry. INTEGRATION owns current sanitizer/full/install evidence.
- [x] **INTEGRATION (independently reviewed and parent-accepted):** public example, installed consumers/smoke orchestration, support docs and consolidated current-source validation/manifests are complete. Focused closure corrected selector dispatch and evidence provenance without production changes.

The API methods all use `submit ~reuse:false`: their complete Bridge request, core ownership and cleanup stay inside one worker and only owned values return. `execute` keeps the accepted SQL-only reuse rule. Relative Parquet path conversion occurs in the worker owner. No core, FFI, Async, Eio lifecycle, backend, or package configuration changes are part of API.

Stage4f acceptance: combined reviewer `59307997-b225-4dbb-98df-744731dd1e45` found no production defect; focused reviewers `7057a115-4709-4dbd-a199-a566e5a7da30` and `5c4a469f-2659-400c-ac25-bb1a193cb680` closed all evidence and selector-dispatch findings. Parent passed the nine checks recorded in `.local/stage4f/parent-evidence-index.json`, including one full forced regression, all examples, isolated four-package smoke, strict C and 370-file source preservation. Stage4 is complete for planned scope; Stage5 remains unimplemented. No publication is authorized.
